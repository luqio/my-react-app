function TestLint(props: {
  UserName: string;
  phoneNumber: number;
  hidden: boolean;
}) {
  const { UserName, phoneNumber, hidden } = props;
  return <div>TestLint</div>;
}

export default TestLint;
